# n8n-node-mocker configuration
# Copy this file to config.yaml and adjust as needed.

port: 9090
fixturesDir: ./fixtures
caDir: ~/.n8n-node-mocker

# What to do when no fixture matches a request in mock mode:
#   auto  - return smart defaults (200 OK with empty JSON) so n8n keeps working
#   error - return 501 (original behavior, useful for strict testing)
fallbackMode: auto

# Per-service signing secrets.
# These must match the secrets configured in n8n credentials
# for signature verification to pass.
# Default: "test" for most services. Stripe uses "whsec_test" to match
# the webhook endpoint secret returned by the Stripe fixture.
services:
  linear:
    signingSecret: "test"
  typeform:
    signingSecret: "test"
  figma:
    signingSecret: "test"
  gitlab:
    signingSecret: "test"
  trello:
    signingSecret: "test"
  twilio:
    signingSecret: "test"
  asana:
    signingSecret: "test"
  netlify:
    signingSecret: "test"
  acuityscheduling:
    signingSecret: "test"
  awssns:
    signingSecret: "test"
  cal:
    signingSecret: "test"
  calendly:
    signingSecret: "test"
  customerio:
    signingSecret: "test"
  formstack:
    signingSecret: "test"
  mailerlite:
    signingSecret: "test"
  mautic:
    signingSecret: "test"
  onfleet:
    signingSecret: "test"
  stripe:
    signingSecret: "whsec_test"
  taiga:
    signingSecret: "test"
